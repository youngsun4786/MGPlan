---
phase: 05-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - tsconfig.sw.json
  - app/lib/database.types.ts
autonomous: true
requirements: [FIX-02]
must_haves:
  truths:
    - "Deno edge functions are excluded from tsc compilation"
    - "Service worker has its own tsconfig with WebWorker lib"
    - "Database types reflect the live Supabase schema including screenshot_url and push_subscriptions"
  artifacts:
    - path: "tsconfig.json"
      provides: "Root TS config excluding Deno and SW files"
      contains: "supabase/functions"
    - path: "tsconfig.sw.json"
      provides: "Separate SW tsconfig with WebWorker lib and path aliases"
      contains: "WebWorker"
    - path: "app/lib/database.types.ts"
      provides: "Fresh Supabase-generated types"
      contains: "screenshot_url"
  key_links:
    - from: "tsconfig.json"
      to: "supabase/functions/**"
      via: "exclude array"
      pattern: "exclude.*supabase/functions"
    - from: "tsconfig.sw.json"
      to: "app/sw.ts"
      via: "include array"
      pattern: "include.*app/sw.ts"
    - from: "app/lib/supabase.browser.ts"
      to: "app/lib/database.types.ts"
      via: "Database type import"
      pattern: "import.*Database.*database\\.types"
    - from: "app/lib/supabase.server.ts"
      to: "app/lib/database.types.ts"
      via: "Database type import"
      pattern: "import.*Database.*database\\.types"
---

<objective>
Fix TypeScript configuration and regenerate database types to eliminate ~46 of 54 tsc errors.

Purpose: The majority of type errors stem from three root causes: stale Supabase types (23 errors), missing WebWorker lib for service worker (12 errors), and Deno edge functions included in Node tsc (11 errors). Fixing these config-level issues cascades fixes across the codebase.

Output: Updated tsconfig.json, new tsconfig.sw.json, regenerated database.types.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bug-fixes/05-CONTEXT.md
@.planning/phases/05-bug-fixes/05-RESEARCH.md

@tsconfig.json
@app/sw.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tsconfig — exclude Deno functions, exclude SW, create SW tsconfig</name>
  <files>tsconfig.json, tsconfig.sw.json</files>
  <read_first>
    - tsconfig.json (current config — must see full structure before editing)
    - app/sw.ts (verify SW file path and what globals it uses)
  </read_first>
  <action>
    **Per D-03:** Add `"exclude"` array to `tsconfig.json` with `"supabase/functions/**"` and `"app/sw.ts"`.

    **Per D-04:** Create `tsconfig.sw.json` as an independent tsconfig (no project references, no composite — simpler approach per RESEARCH.md recommendation). Contents:

    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "jsx": "react-jsx",
        "module": "ESNext",
        "moduleResolution": "bundler",
        "allowImportingTsExtensions": true,
        "verbatimModuleSyntax": true,
        "lib": ["ES2022", "WebWorker"],
        "types": ["vite/client"],
        "baseUrl": ".",
        "paths": { "~/*": ["./app/*"] },
        "strict": true,
        "noEmit": true,
        "skipLibCheck": true,
        "noUnusedLocals": true,
        "noUnusedParameters": true,
        "noFallthroughCasesInSwitch": true,
        "noUncheckedSideEffectImports": true
      },
      "include": ["app/sw.ts"]
    }
    ```

    The SW tsconfig mirrors root compilerOptions except: `lib` is `["ES2022", "WebWorker"]` instead of `["ES2022", "DOM", "DOM.Iterable"]`, and `include` is only `["app/sw.ts"]`.

    IMPORTANT — Addresses review concern (path aliases for SW IntelliSense): The `tsconfig.sw.json` MUST include `"baseUrl": "."` and `"paths": { "~/*": ["./app/*"] }` so that editors can resolve `~/` imports in `app/sw.ts`. Without this, IDEs lose IntelliSense for the SW file after it is excluded from the main tsconfig.

    **Pre-check verification (addresses review concern):** After editing tsconfig.json and creating tsconfig.sw.json, run `npx tsc --noEmit 2>&1 | tail -1` to capture the new error count. Compare against the baseline of 54 errors. Expected: error count drops to ~31 (Deno: -11, SW: -12). Record the exact count in a comment for the summary. If the count did NOT drop, something is wrong — re-read tsconfig.json and verify the exclude array is correct.

    Also run `npx tsc --noEmit -p tsconfig.sw.json 2>&1 | tail -5` to confirm SW compiles cleanly or identify remaining SW-specific errors.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -3 && echo "---SW---" && npx tsc --noEmit -p tsconfig.sw.json 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.json contains `"exclude": ["supabase/functions/**", "app/sw.ts"]`
    - tsconfig.sw.json exists and contains `"WebWorker"` in lib array
    - tsconfig.sw.json contains `"include": ["app/sw.ts"]`
    - tsconfig.sw.json contains `"paths": { "~/*": ["./app/*"] }` (review concern: path aliases for IDE support)
    - `npx tsc --noEmit` output does NOT contain `supabase/functions` errors
    - `npx tsc --noEmit` output does NOT contain `app/sw.ts` errors
    - `npx tsc --noEmit` error count is lower than 54 (pre-check confirms drop)
  </acceptance_criteria>
  <done>Root tsc no longer reports errors for Deno edge functions or service worker. SW has its own clean tsconfig with path aliases for IDE support. Error count confirmed dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate Supabase database types</name>
  <files>app/lib/database.types.ts</files>
  <read_first>
    - app/lib/database.types.ts (see current stale types — verify it lacks screenshot_url and push_subscriptions)
    - app/lib/supabase.browser.ts (verify it imports Database type from database.types — addresses review concern about client importing regenerated types)
    - app/lib/supabase.server.ts (verify it imports Database type from database.types — addresses review concern about client importing regenerated types)
  </read_first>
  <action>
    **Per D-05:** Regenerate database types from the live Supabase database.

    **Pre-check (addresses review concern about Supabase CLI auth):** The Supabase project is NOT currently linked (no `supabase/.temp/project-ref` file exists). Before running gen types, try linking:

    1. First attempt: `npx supabase gen types --lang=typescript --linked > app/lib/database.types.ts`
    2. If that fails with "not linked" error:
       - Check for project ref in env: `echo $SUPABASE_PROJECT_REF` or check `.env` files
       - Try: `npx supabase projects list` to find the project ref
       - Use explicit project ID: `npx supabase gen types --lang=typescript --project-id <ref> > app/lib/database.types.ts`
    3. If all automated approaches fail, this becomes a checkpoint — inform the user to run `npx supabase link --project-ref <ref>` and then retry.

    After regeneration, immediately verify:
    1. The generated file contains `screenshot_url` (confirms tasks table is current)
    2. The generated file contains `push_subscriptions` (confirms push table exists)
    3. The generated file still exports the `Database` type (both `app/lib/supabase.browser.ts` and `app/lib/supabase.server.ts` import `Database` from this file — addresses review concern about client using regenerated types)
    4. If the generated file does NOT have the helper type exports at the bottom (`Task`, `TaskInsert`, `TaskUpdate`, `Staff`), re-add them:
       ```typescript
       export type Task = Database['public']['Tables']['tasks']['Row']
       export type TaskInsert = Database['public']['Tables']['tasks']['Insert']
       export type TaskUpdate = Database['public']['Tables']['tasks']['Update']
       export type Staff = Database['public']['Tables']['staff']['Row']
       ```

    **Post-regen tsc check (addresses review concern):** Run `npx tsc --noEmit 2>&1 | tail -1` and compare to the count from Task 1. Error count should drop further (target: ~8 remaining from Group D localized fixes). If error count goes UP, inspect new errors — the schema may have columns the code doesn't expect. Address any new mismatches before proceeding.
  </action>
  <verify>
    <automated>grep -c "screenshot_url" app/lib/database.types.ts && grep -c "push_subscriptions" app/lib/database.types.ts && npx tsc --noEmit 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - app/lib/database.types.ts contains the string `screenshot_url`
    - app/lib/database.types.ts contains the string `push_subscriptions`
    - app/lib/database.types.ts exports `Database` type (line 1: `export type Database =`)
    - Supabase browser client (`app/lib/supabase.browser.ts`) imports `Database` from `./database.types` (already verified, no change needed)
    - Supabase server client (`app/lib/supabase.server.ts`) imports `Database` from `./database.types` (already verified, no change needed)
    - `npx tsc --noEmit` error count is lower than post-Task-1 count (target: ~8 remaining)
  </acceptance_criteria>
  <done>Database types are fresh from live schema. Server functions and components using Supabase client no longer hit `never` types on table operations. Both Supabase clients use the regenerated Database type.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npx tsc --noEmit 2>&1 | tail -1` — should show ~8 or fewer errors (Group D localized fixes remain)
- `npx tsc --noEmit -p tsconfig.sw.json` — should show 0 errors
- `npm run test` — existing tests still pass (regression check)
</verification>

<success_criteria>
- Root tsc error count reduced from 54 to ~8 or fewer
- SW tsc reports 0 errors
- No Deno function errors in tsc output
- No `never` type errors on Supabase table operations
- Existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/05-bug-fixes/05-01-SUMMARY.md`
</output>
