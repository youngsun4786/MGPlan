---
phase: 05-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/components/TaskForm.tsx
  - app/components/TaskBoard.tsx
  - app/components/TaskRow.tsx
  - app/components/StatusBadge.tsx
  - app/lib/push.ts
  - app/routes/signup.tsx
  - tests/components/TaskBoard.test.tsx
  - tests/components/TaskRow.test.tsx
autonomous: true
requirements: [FIX-01, FIX-02]
must_haves:
  truths:
    - "Mobile staff can tap photo upload and choose from photo library or camera"
    - "Running npx tsc --noEmit produces zero errors"
    - "Running npx tsc --noEmit -p tsconfig.sw.json produces zero errors"
    - "All existing tests pass"
    - "Clicking status badge does not trigger the parent row click handler"
  artifacts:
    - path: "app/components/TaskForm.tsx"
      provides: "Photo upload without capture restriction, 'Attach Photo' label"
      contains: "Attach Photo"
    - path: "app/components/TaskBoard.tsx"
      provides: "Clean component with no unused variables"
    - path: "app/components/TaskRow.tsx"
      provides: "handleBadgeClick with stopPropagation preserved"
    - path: "app/components/StatusBadge.tsx"
      provides: "onClick type updated to accept MouseEvent"
      contains: "React.MouseEvent"
    - path: "app/lib/push.ts"
      provides: "Uint8Array type compatible with BufferSource"
    - path: "app/routes/signup.tsx"
      provides: "Link components with search prop"
      contains: "search={{}}"
    - path: "tests/components/TaskBoard.test.tsx"
      provides: "Test fixtures with screenshot_url and typed Sets"
      contains: "screenshot_url"
    - path: "tests/components/TaskRow.test.tsx"
      provides: "Test fixtures with screenshot_url"
      contains: "screenshot_url"
  key_links:
    - from: "app/components/TaskForm.tsx"
      to: "mobile OS file picker"
      via: "file input without capture attribute"
      pattern: "type=\"file\".*accept=.*className=\"hidden\""
    - from: "app/components/TaskRow.tsx"
      to: "app/components/StatusBadge.tsx"
      via: "onClick prop with MouseEvent"
      pattern: "onClick.*handleBadgeClick"
---

<objective>
Fix all remaining localized TypeScript errors and the mobile photo picker bug.

Purpose: After Plan 01 eliminated ~46 config-level errors, ~8 localized type mismatches remain across individual files. This plan fixes those plus the FIX-01 photo picker issue, achieving the phase goal of zero tsc errors and working mobile photo upload.

Output: All affected files fixed, `npx tsc --noEmit` passes with 0 errors
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bug-fixes/05-CONTEXT.md
@.planning/phases/05-bug-fixes/05-RESEARCH.md
@.planning/phases/05-bug-fixes/05-01-SUMMARY.md

@app/components/TaskForm.tsx
@app/components/TaskBoard.tsx
@app/components/TaskRow.tsx
@app/components/StatusBadge.tsx
@app/lib/push.ts
@app/routes/signup.tsx
@tests/components/TaskBoard.test.tsx
@tests/components/TaskRow.test.tsx

<interfaces>
<!-- Key type contracts the executor needs -->

From app/components/StatusBadge.tsx:
```typescript
interface StatusBadgeProps {
  status: TaskStatus
  onClick?: () => void       // CURRENT — must change to (e: React.MouseEvent<HTMLButtonElement>) => void
  disabled?: boolean
}
```

From app/components/TaskRow.tsx (line 29-31):
```typescript
function handleBadgeClick(e: React.MouseEvent) {
  e.stopPropagation()        // IMPORTANT: This IS used — prevents row click when tapping badge
  onStatusChange(task.id, NEXT_STATUS[task.status])
}
```

From app/lib/supabase.browser.ts:
```typescript
import type { Database } from './database.types'
export const supabaseBrowserClient = createBrowserClient<Database>(...)
```

From app/lib/constants.ts:
```typescript
export type TaskStatus = 'open' | 'in_progress' | 'done'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix photo picker and all localized TypeScript errors</name>
  <files>
    app/components/TaskForm.tsx,
    app/components/TaskBoard.tsx,
    app/components/TaskRow.tsx,
    app/components/StatusBadge.tsx,
    app/lib/push.ts,
    app/routes/signup.tsx,
    tests/components/TaskBoard.test.tsx,
    tests/components/TaskRow.test.tsx
  </files>
  <read_first>
    - app/components/TaskForm.tsx (find capture="environment" ~line 302, find "Attach Screenshot" ~line 295)
    - app/components/TaskBoard.tsx (find unused hasActiveFilters variable ~line 23)
    - app/components/TaskRow.tsx (find handleBadgeClick with MouseEvent param ~line 29 — VERIFY it uses e.stopPropagation())
    - app/components/StatusBadge.tsx (find onClick type in StatusBadgeProps interface ~line 6 — must update type to accept event)
    - app/lib/push.ts (find urlBase64ToUint8Array and applicationServerKey ~line 12)
    - app/routes/signup.tsx (find Link to="/login" ~lines 86,197)
    - tests/components/TaskBoard.test.tsx (find mock task objects ~line 63, find Set() ~lines 68,73,80, find unused mockOn import ~line 5)
    - tests/components/TaskRow.test.tsx (find mock task objects ~lines 23,30,37,44)
    - app/lib/constants.ts (find TaskStatus type export — needed for typed Set in tests)
  </read_first>
  <action>
    Apply all localized fixes. Each fix is independent — order does not matter.

    **FIX-01 — Photo picker (per D-01, D-02):**

    In `app/components/TaskForm.tsx`:
    1. Remove the `capture="environment"` attribute from the file input element (~line 302). The input should be:
       ```tsx
       <input
         ref={fileInputRef}
         type="file"
         accept={ACCEPTED_EXTENSIONS}
         className="hidden"
         onChange={handleFileSelected}
       />
       ```
    2. Change the button label from `Attach Screenshot` to `Attach Photo` (~line 295).

    **FIX-02 — Localized type fixes (per D-06, D-07):**

    In `app/components/TaskBoard.tsx`:
    - Remove the unused `hasActiveFilters` variable (~line 23). If it's a destructured const, remove just that binding.

    In `app/components/StatusBadge.tsx` (addresses review concern about stopPropagation):
    - **CRITICAL:** The `handleBadgeClick` function in TaskRow.tsx calls `e.stopPropagation()` to prevent the row click handler from firing when the status badge is tapped. Do NOT just remove the `e` parameter — that would break badge click isolation.
    - Update the `StatusBadgeProps` interface `onClick` type from `() => void` to `(e: React.MouseEvent<HTMLButtonElement>) => void`:
      ```typescript
      interface StatusBadgeProps {
        status: TaskStatus
        onClick?: (e: React.MouseEvent<HTMLButtonElement>) => void
        disabled?: boolean
      }
      ```
    - Update the button's onClick in StatusBadge to pass the event through. Change line 44 from:
      ```tsx
      onClick={onClick}
      ```
      to:
      ```tsx
      onClick={onClick}
      ```
      (No change needed here — the native button onClick already passes the event to the handler. The type change in the interface is sufficient.)

    In `app/components/TaskRow.tsx`:
    - Keep `handleBadgeClick(e: React.MouseEvent)` as-is. The `e.stopPropagation()` call is necessary. The type error is resolved by the StatusBadge onClick type update above.

    In `app/lib/push.ts`:
    - Fix the `Uint8Array<ArrayBufferLike>` not assignable to `BufferSource` error on the `applicationServerKey` line. Cast the return value of `urlBase64ToUint8Array` as `Uint8Array<ArrayBuffer>`:
      ```typescript
      applicationServerKey: urlBase64ToUint8Array(vapidPublicKey) as Uint8Array<ArrayBuffer>,
      ```
      This cast is safe — `Uint8Array.from()` always creates a regular ArrayBuffer-backed array.

    In `app/routes/signup.tsx` (per D-06):
    - Find all `<Link to="/login"` components (~lines 86 and 197). Add the `search` prop:
      ```tsx
      <Link to="/login" search={{}} className="...">
      ```
      The `/login` route has a `validateSearch` schema with an optional `expired` param that defaults to undefined, so `search={{}}` satisfies the type.
    - **Addresses review concern (TanStack Router runtime warnings):** After making this change, the dev server should be started (`npm run dev`) and navigating to `/signup` should NOT produce console warnings about search params. The executor should check the browser console after the full tsc verification. If `search={{}}` causes warnings, switch to `search={{ expired: undefined }}` instead.

    In `tests/components/TaskBoard.test.tsx` (per D-07):
    - Remove the unused `mockOn` import (~line 5).
    - Add `screenshot_url: null` to every mock task object (~line 63 and any others).
    - Type the Set constructors: change `new Set()` to `new Set<TaskStatus>()` (~lines 68, 73, 80). Import `TaskStatus` type:
      ```typescript
      import type { TaskStatus } from '~/lib/constants'
      ```

    In `tests/components/TaskRow.test.tsx` (per D-07):
    - Add `screenshot_url: null` to every mock task object (~lines 23, 30, 37, 44).

    After all fixes, run `npx tsc --noEmit && npx tsc --noEmit -p tsconfig.sw.json` — both must report 0 errors.
    Then run `npm run test` — all tests must pass.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx tsc --noEmit -p tsconfig.sw.json && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - app/components/TaskForm.tsx does NOT contain `capture="environment"`
    - app/components/TaskForm.tsx contains `Attach Photo` (not `Attach Screenshot`)
    - app/components/TaskBoard.tsx does NOT contain `hasActiveFilters`
    - app/components/StatusBadge.tsx contains `React.MouseEvent<HTMLButtonElement>` in onClick type
    - app/components/TaskRow.tsx `handleBadgeClick` STILL calls `e.stopPropagation()` (preserved, not removed)
    - app/lib/push.ts contains `as Uint8Array<ArrayBuffer>` (or equivalent fix)
    - app/routes/signup.tsx all `Link to="/login"` have `search={{}}` prop
    - tests/components/TaskBoard.test.tsx contains `screenshot_url: null`
    - tests/components/TaskBoard.test.tsx contains `new Set<TaskStatus>()`
    - tests/components/TaskBoard.test.tsx does NOT contain `mockOn` import
    - tests/components/TaskRow.test.tsx contains `screenshot_url: null`
    - `npx tsc --noEmit` exits with code 0
    - `npx tsc --noEmit -p tsconfig.sw.json` exits with code 0
    - `npm run test` exits with code 0
  </acceptance_criteria>
  <done>Zero TypeScript errors across the entire codebase. Mobile photo upload allows library selection. All tests pass. Status badge click isolation preserved via proper type fix.</done>
</task>

</tasks>

<verification>
Phase-level verification:
1. `npx tsc --noEmit && npx tsc --noEmit -p tsconfig.sw.json` — 0 errors (FIX-02 complete)
2. `npm run test` — all tests pass (regression)
3. `grep -n "capture=" app/components/TaskForm.tsx` — no results (FIX-01 attribute removed)
4. `grep -n "Attach Photo" app/components/TaskForm.tsx` — found (FIX-01 label updated)
5. `grep -n "stopPropagation" app/components/TaskRow.tsx` — still present (review concern: badge click isolation preserved)
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0 with zero errors
- `npx tsc --noEmit -p tsconfig.sw.json` exits 0 with zero errors
- `npm run test` passes
- TaskForm.tsx has no `capture` attribute on file input
- TaskForm.tsx shows "Attach Photo" label
- TaskRow.tsx stopPropagation behavior preserved
</success_criteria>

<output>
After completion, create `.planning/phases/05-bug-fixes/05-02-SUMMARY.md`
</output>
