---
phase: 04-filters-search-hardening
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - app/components/FilterBar.tsx
  - app/components/TaskBoard.tsx
  - .planning/phases/04-filters-search-hardening/HARDENING-CHECKLIST.md
autonomous: false
requirements: [FILT-01, FILT-02, FILT-03, FILT-04]

must_haves:
  truths:
    - "Filter bar is visible, sticky, and usable on the live board"
    - "All filter types work: status, request type, date range, search, sort"
    - "Accessibility attributes are present (aria-pressed, aria-label)"
    - "Error handling uses toast notifications per D-09"
    - "RLS policies are verified correct for the flat team model"
  artifacts:
    - path: ".planning/phases/04-filters-search-hardening/HARDENING-CHECKLIST.md"
      provides: "RLS audit, cross-browser checklist, Lighthouse checklist, env var audit"
  key_links:
    - from: "app/components/FilterBar.tsx"
      to: "app/components/TaskBoard.tsx"
      via: "both rendered in board page, FilterBar controls what TaskBoard displays"
      pattern: "FilterBar|applyFilters"
---

<objective>
Production hardening pass and visual verification. Add accessibility polish to FilterBar, verify error handling uses toasts per D-09, create the hardening checklist (RLS audit, cross-browser, Lighthouse, env vars), and checkpoint for human verification.

Purpose: Ensure the filter UI meets accessibility and production quality standards, and document the hardening audit.
Output: Polished FilterBar with a11y, hardening checklist, human-verified working filters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-filters-search-hardening/04-CONTEXT.md
@.planning/phases/04-filters-search-hardening/04-RESEARCH.md
@.planning/phases/04-filters-search-hardening/04-01-SUMMARY.md
@.planning/phases/04-filters-search-hardening/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accessibility polish and hardening checklist</name>
  <files>app/components/FilterBar.tsx, app/components/TaskBoard.tsx, .planning/phases/04-filters-search-hardening/HARDENING-CHECKLIST.md</files>
  <read_first>app/components/FilterBar.tsx, app/components/TaskBoard.tsx, app/routes/_auth/index.tsx, supabase/migrations/0001_initial.sql, supabase/migrations/0002_push_subscriptions.sql</read_first>
  <action>
    **Part A: Accessibility audit on FilterBar.tsx**

    Verify and fix if missing:
    - Every filter chip button has `aria-pressed={active}` attribute
    - Search input has `aria-label="Search by client name or phone"`
    - Sort toggle button has `aria-label` that reads current state: "Sort: oldest first" or "Sort: newest first"
    - Date inputs have associated labels (use `<label>` elements or `aria-label`)
    - All interactive elements have min touch target 44x44px (check `min-h-[44px]` or equivalent)
    - Filter chip container has `role="group"` and `aria-label="Filter by status"` / `aria-label="Filter by request type"`
    - Color contrast: active chips (bg-blue-600 text-white) pass WCAG AA. Inactive chips (bg-slate-100 text-slate-700) pass WCAG AA.

    **Part B: Verify error handling per D-09**

    Check `app/components/TaskBoard.tsx` -- the existing `handleStatusChange` catch block should use `toast.error()` from sonner instead of silently refetching. If it currently just refetches without user feedback, add:
    ```typescript
    import { toast } from 'sonner'
    // In the catch block:
    toast.error('Failed to update task status. Please try again.')
    ```
    Keep the refetch for data recovery, but add the toast for user awareness per D-09.

    **Part C: Create HARDENING-CHECKLIST.md**

    Create `.planning/phases/04-filters-search-hardening/HARDENING-CHECKLIST.md` with four sections based on the research findings:

    **1. RLS Audit** -- Based on reading the actual migration files:
    - tasks table: 4 policies (SELECT, INSERT, UPDATE, DELETE) all use `TO authenticated USING (true)` -- CORRECT for flat team model, no role hierarchy needed
    - staff table: SELECT all authenticated, INSERT own record only (id = auth.uid()) -- CORRECT
    - screenshots storage: INSERT and SELECT for authenticated with bucket_id = 'screenshots' -- CORRECT
    - push_subscriptions: Read the 0002 migration, document what policies exist. Flag if staff can delete other staff's subscriptions (potential gap from research).
    - Verdict: PASS / NEEDS FIX with specific recommendation

    **2. Cross-Browser Smoke Test Checklist:**
    - [ ] iOS Safari: Filter bar renders, chips tappable (44px targets), date inputs open native picker, search input no zoom (text-base), sticky positioning works
    - [ ] Android Chrome: Same as iOS checks
    - [ ] macOS Chrome: All interactions work with mouse, keyboard tab navigation through filters
    - [ ] Windows Chrome: Same as macOS

    **3. Lighthouse Audit Checklist:**
    - [ ] Run Lighthouse on mobile emulation
    - [ ] Performance score >= 90
    - [ ] PWA score >= 90
    - [ ] Accessibility score >= 90
    - [ ] Best Practices score >= 90
    - Key items: aria-pressed on chips, aria-labels on inputs, color contrast, no console errors

    **4. Environment Variable Audit:**
    - SUPABASE_URL / VITE_SUPABASE_URL: Public, RLS-enforced -- OK
    - SUPABASE_ANON_KEY / VITE_SUPABASE_ANON_KEY: Public, RLS-enforced -- OK
    - VITE_VAPID_PUBLIC_KEY: Public key, safe to expose -- OK
    - VAPID_PRIVATE_KEY: Edge Function only (Deno.env) -- OK, never in client
    - SUPABASE_SERVICE_ROLE_KEY: Edge Function only -- OK, never in client
    - No API keys (Anthropic/OpenAI) found in .env or codebase -- OK
    - Secrets rotation: VAPID keys (re-subscribe all clients), SERVICE_ROLE_KEY (update edge function), JWT secret (Supabase dashboard)
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - FilterBar.tsx has `aria-pressed` on every chip button
    - FilterBar.tsx search input has `aria-label`
    - FilterBar.tsx sort button has descriptive `aria-label`
    - TaskBoard.tsx handleStatusChange catch block includes `toast.error()`
    - `.planning/phases/04-filters-search-hardening/HARDENING-CHECKLIST.md` exists with RLS audit, cross-browser, Lighthouse, and env var sections
    - All tests still pass
  </acceptance_criteria>
  <done>Accessibility attributes verified/added, error handling uses toasts per D-09, hardening checklist documents RLS audit findings, cross-browser test plan, Lighthouse targets, and env var audit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification</name>
  <files>app/components/FilterBar.tsx, app/components/TaskBoard.tsx, app/routes/_auth/index.tsx</files>
  <read_first>app/components/FilterBar.tsx, app/components/TaskBoard.tsx, app/routes/_auth/index.tsx</read_first>
  <action>
    Human verification checkpoint. Start the dev server with `npm run dev` and present the verification steps below to the user. No code changes -- this is a visual/functional verification gate.
  </action>
  <what-built>
    Complete filter/search/sort system for the task board:
    - Sticky filter bar below header with status chips, request type chips, date range, search input, sort toggle
    - Live search filtering as you type
    - AND-combined filters
    - Two distinct empty states ("No tasks yet" vs "No tasks match your filters")
    - Accessibility attributes for screen readers
    - Toast error handling
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open http://localhost:3000 and log in
    3. **Filter bar visible:** Confirm sticky filter bar appears below the header with chips, search, and sort toggle
    4. **Status filter (FILT-01):** Tap "Open" chip -- only open tasks shown. Tap again to deselect -- all tasks return. Tap "Done" -- only done tasks shown.
    5. **Request type filter (FILT-02):** Tap "New Booking" -- only new booking tasks shown. Combine with status: tap "Open" + "New Booking" -- only open new bookings shown (AND logic per D-02).
    6. **Date range filter (FILT-03):** Set "From" date to tomorrow -- tasks created before tomorrow should disappear. Clear the date -- tasks return.
    7. **Search (FILT-04):** Type a client name -- list narrows instantly per keystroke (D-06). Type a phone number -- matches phone field. Clear search -- all tasks return.
    8. **Sort (D-08):** Tap sort toggle -- order flips between oldest-first and newest-first. Arrow icon changes direction.
    9. **No results (D-10):** Apply filters that match nothing -- "No tasks match your filters" message appears with "Clear filters" button. Tap "Clear filters" -- all tasks return.
    10. **Mobile (375px):** Resize to 375px width -- filter bar should be usable, chips horizontally scrollable
    11. **Realtime:** Open a second browser tab, create a task there -- it should appear on the first tab respecting active filters
  </how-to-verify>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - User confirms filter bar is visible and sticky
    - User confirms all four filter types work (status, request type, date range, search)
    - User confirms sort toggle works
    - User confirms "No tasks match your filters" empty state appears with Clear button
    - User confirms mobile layout is usable at 375px
  </acceptance_criteria>
  <done>User has approved the filter/search/sort system as functional and usable.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All tests pass: `npx vitest run`
- Filter system works end-to-end on the live board
- Accessibility attributes present for Lighthouse
- Hardening checklist created with audit findings
- Human verified the UI is functional and usable
</verification>

<success_criteria>
- Filter bar is sticky, visible, and functional on all screen sizes
- All four FILT requirements verified by human testing
- Accessibility ready for Lighthouse 90+ target
- Hardening documentation complete
- User approves the visual/functional checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04-filters-search-hardening/04-03-SUMMARY.md`
</output>
